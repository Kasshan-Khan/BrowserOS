// Auth constants — safe to import in middleware (no heavy dependencies)
export const SESSION_COOKIE = '__bos_session';
export const CSRF_HEADER = 'x-csrf-token';
export const CSRF_COOKIE = '__bos_csrf';
